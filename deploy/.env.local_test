# ==========================================
# DEPLOYMENT CONFIG (LOCAL TEST)
# ==========================================
DOMAIN=localhost
ACME_EMAIL=admin@localhost

# ==========================================
# DATABASE
# ==========================================
DB_NAME=control_room
DB_USER=control_room_user
DB_PASSWORD=local_test_password
DB_HOST=db
DB_PORT=5432

# ==========================================
# REDIS
# ==========================================
REDIS_HOST=redis
REDIS_PORT=6379

# ==========================================
# DJANGO SETTINGS
# ==========================================
# Force production settings logic, but we will override specific behaviors
DJANGO_ENV=production
DEBUG=True
SECRET_KEY=django-insecure-local-test-key-12345
ALLOWED_HOSTS=*
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:3000
CSRF_TRUSTED_ORIGINS=http://localhost,http://localhost:3000

# ==========================================
# FRONTEND SETTINGS
# ==========================================
NEXT_PUBLIC_API_URL=http://localhost:8000/api
NEXT_PUBLIC_WS_URL=ws://localhost:8000
# Internal URL for Next.js Server Side Rendering (Docker Network)
DJANGO_API_BASE=http://backend:8000
